# Local working notes AND session logs (_local/_session-logs/) - never tracked
_local/

# Tool state directories
.memsearch/
.impeccable/
.superpowers/

# Rust build output
target/

# Backup artifacts produced by awt's own snapshot step during development
*.bak

# OS and editor noise
.DS_Store
Thumbs.db
desktop.ini

# NOTE: session logs live in _local/_session-logs/ and are deliberately NOT tracked.

# agent worktrees (transient)
.claude/worktrees/
